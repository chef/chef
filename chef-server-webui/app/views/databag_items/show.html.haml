.block#block-tables
  .content
    %h2.title= "Databag Item: #{@databag_item_name}"
    .inner
      = partial('navigation', :active => 'show')
    .content
      .inner
        .accordion
          = build_tree('attrs', @dababag_item, @databag_item)
