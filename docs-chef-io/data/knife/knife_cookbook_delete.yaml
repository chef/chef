---
banner: knife cookbook delete COOKBOOK VERSION (options)
all:
  short: "-a"
  long: "--all"
  boolean: true
  description: Delete all versions of the cookbook.
purge:
  short: "-p"
  long: "--purge"
  boolean: true
  description: Permanently remove files from backing data store.
