---
name: kitchen

"on":
  pull_request_target:
    branches:
      - main
  push:
    branches:
      - main

concurrency:
  group: kitchen-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

defaults:
  run:
    working-directory: kitchen-tests

jobs:
  workflow_guard:
    if: github.event_name == 'pull_request_target'
    uses: chef/chef/.github/workflows/workflow-change-guard.yml@main
    permissions:
      contents: read

  win_x86_64:
    needs: workflow_guard
    permissions:
      contents: read
    # temporarily adding this back for inspec testing until we can get Kitchen Enterprise working for Windows.
    strategy:
      fail-fast: false
      matrix:
        os:
          - windows-2025
          - windows-2022
    runs-on: ${{ matrix.os }}
    env:
      CHEF_LICENSE: accept-no-persist
      KITCHEN_LOCAL_YAML: kitchen.exec.windows.yml
      HAB_ORIGIN: gha
      HAB_BLDR_CHANNEL: base-2025
      HAB_REFRESH_CHANNEL: base-2025
      HAB_AUTH_TOKEN: ${{ secrets.HAB_AUTH_TOKEN }}
    steps:
      - name: Checkout PR code
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
          clean: true

      - name: Install Habitat CLI
        shell: pwsh
        run: |
          Invoke-WebRequest -Uri https://raw.githubusercontent.com/habitat-sh/habitat/main/components/hab/install.ps1 -OutFile install-habitat.ps1
          .\install-habitat.ps1
          $env:PATH = "C:\Program Files\Habitat;C:\hab\bin;C:\opscode\chef-workstation\bin;C:\opscode\chef-workstation\embedded\bin;" + $env:PATH
          echo "PATH=$env:PATH" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          hab license accept
          hab --version

      - name: Generate Habitat Origin Key
        shell: pwsh
        run: hab origin key generate "$env:HAB_ORIGIN"

      - name: Build Habitat Package
        shell: pwsh
        working-directory: .
        run: |
          # Build from repo root, not kitchen-tests
          Write-Host "I am in the following directory before building: $(Get-Location)"
          # cd ..
          hab pkg build habitat
          $pkg_path = Get-ChildItem -Path "results" -Filter "$env:HAB_ORIGIN-chef-infra-client-*.hart" |
                      Sort-Object LastWriteTime -Descending |
                      Select-Object -First 1

          if ($pkg_path) {
              Write-Host "✓ Built package: $($pkg_path.Name)"
          } else {
              Write-Error "× No package built"
              Get-ChildItem -Path "results" -ErrorAction SilentlyContinue
              Exit 1
          }

      - name: Install Chef Infra Client from Habitat Package
        shell: pwsh
        working-directory: .
        run: |
          # cd ..
          $pkg_path = Get-ChildItem -Path "results" -Filter "$env:HAB_ORIGIN-chef-infra-client-*.hart" |
                      Sort-Object LastWriteTime -Descending |
                      Select-Object -First 1

          Write-Host "Installing package: $($pkg_path.FullName)"

          hab pkg install $pkg_path.FullName --binlink --force

          # Verify installation
          Write-Host "Installed Chef Infra Client version:"
          chef-client --version

      - name: Install Chef Workstation (for kitchen CLI)
        shell: pwsh
        run: choco install -y chef-workstation

      - name: Kitchen Test
        shell: pwsh
        run: kitchen verify end-to-end-${{ matrix.os }}


  # Testing on windows by running habitat package of Infra Client chef-client in local mode.
  # win_x86_64:
  #   name: win_x86_64_hab
  #   if: github.event.pull_request.head.repo.full_name == github.repository || github.event_name == 'push' # Don't run on external PRs for now.
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       os:
  #         - windows-2022
  #         - windows-2025
  #   runs-on: ${{ matrix.os }}
  #   env:
  #     CHEF_LICENSE: accept-no-persist
  #     CHEF_LICENSE_KEY: free-79df705d-b685-419a-8b68-88401f74ff72-3999
  #     HAB_ORIGIN: gha
  #     HAB_BLDR_CHANNEL: base-2025
  #     HAB_REFRESH_CHANNEL: base-2025
  #     HAB_AUTH_TOKEN: ${{ secrets.HAB_AUTH_TOKEN }}
  #   steps:
  #   - name: Checkout PR code
  #     uses: actions/checkout@v6
  #     with:
  #       ref: ${{ github.event.pull_request.head.sha }}
  #       persist-credentials: false
  #       clean: true
  #   - name: Install Habitat CLI
  #     run: |
  #       # Install Habitat using Chocolatey
  #       choco install -y habitat
  #       refreshenv
  #       # Ensure hab is in PATH
  #       $env:PATH = "C:\Program Files\Habitat;" + $env:PATH
  #       echo "PATH=$env:PATH" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
  #       hab license accept
  #       hab --version
  #   - name: Generate Habitat Origin Key
  #     run: |
  #       hab origin key generate "$env:HAB_ORIGIN"
  #   - name: Install Chef CLI
  #     run: |
  #       hab pkg install --binlink --force chef/chef-cli --channel unstable
  #   - name: Build Habitat Package
  #     run: |
  #       # Move to repo root
  #       cd ..
  #       hab pkg build .

  #       # Find the built package
  #       $pkg_path = Get-ChildItem -Path "results" -Filter "$env:HAB_ORIGIN-chef-infra-client-*.hart" |
  #                   Sort-Object LastWriteTime -Descending |
  #                   Select-Object -First 1

  #       if ($pkg_path) {
  #           Write-Host "Built package: $($pkg_path.Name)"
  #           Copy-Item $pkg_path.FullName -Destination "kitchen-tests\chef-infra-client-latest.hart"
  #       } else {
  #           Write-Error "No package built"
  #           Exit 1
  #       }
  #   - name: Install Chef Infra Client Habitat Package
  #     run: |
  #       hab pkg install chef-infra-client-latest.hart
  #       hab pkg exec $env:HAB_ORIGIN/chef-infra-client chef-client --version
  #   - name: Run end-to-end recipes in Local Mode
  #     run: |
  #       # Install the Policyfile (resolves dependencies)
  #       hab pkg exec chef/chef-cli chef-cli install Policyfile.rb
  #       if ($LASTEXITCODE -ne 0) {
  #           Write-Error "Failed to install Policyfile"
  #           Exit 1
  #       }
  #       # Export the policy and all cookbooks to a sandbox directory
  #       # This is exactly what Test Kitchen does
  #       hab pkg exec chef/chef-cli chef-cli export Policyfile.lock.json policy-export --force
  #       if ($LASTEXITCODE -ne 0) {
  #           Write-Error "Failed to export Policyfile"
  #           Exit 1
  #       }
  #       # Run chef-client in local mode from the exported directory
  #       # Just like Test Kitchen does: cd <export-dir> && chef-client -z
  #       cd policy-export
  #       hab pkg exec $env:HAB_ORIGIN/chef-infra-client chef-client -z
  #       if ($LASTEXITCODE -ne 0) {
  #           Write-Error "Chef Infra Client run failed"
  #           Exit 1
  #       }

#  mac_x86_64:
#    strategy:
#      fail-fast: false
#      matrix:
#        os:
#          - macos-15-intel
#    runs-on: ${{ matrix.os }}
#    env:
#      CHEF_LICENSE: accept-no-persist
#      KITCHEN_LOCAL_YAML: kitchen.exec.macos.yml
#    steps:
#      - name: Check out code
#        uses: actions/checkout@v5
#        with:
#            clean: true
#      - name: Install Chef
#        uses: actionshub/chef-install@main
#        with:
#          version: "25.5.1084"
#      - name: Kitchen Test
#        run: kitchen verify ${{ matrix.os }}
#
  mac_arm64:
    needs: workflow_guard
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-14
          - macos-15
          - macos-26
    runs-on: ${{ matrix.os }}
    env:
      CHEF_LICENSE: accept-no-persist
      KITCHEN_LOCAL_YAML: kitchen.exec.macos.yml
    steps:
      - name: Checkout PR code
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
          clean: true
      - name: Install Chef
        uses: actionshub/chef-install@main
        with:
          version: "25.5.1084"
      - name: Kitchen Test
        run: kitchen verify ${{ matrix.os }}

  # end-to-end recipe testing using test-kitchen-enterprise and habitat package of Infra Client
  docr_lnx_x86_64:
    name: dokr_lnx_x86_64_hab
    needs: workflow_guard
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os:
          - amazonlinux-2023
          - almalinux-8
          - almalinux-9
          - almalinux-10
          - debian-11
          - debian-12
          - debian-13
          - fedora-42
          - opensuse-leap-15
          - oraclelinux-8
          - oraclelinux-9
          - oraclelinux-10
          - rockylinux-8
          - rockylinux-9
          - rockylinux-10
          - ubuntu-2204
          - ubuntu-2404
    runs-on: ubuntu-latest
    env:
      FORCE_FFI_YAJL: ext
      CHEF_LICENSE: accept-no-persist
      KITCHEN_LOCAL_YAML: kitchen.dokken.yml
      HAB_ORIGIN: gha # Set the dummy origin for this Github actions CI flow
      HAB_BLDR_CHANNEL: base-2025 # Explicitly set the builder channel
      HAB_REFRESH_CHANNEL: base-2025 # Explicitly set the refresh channel
      HAB_AUTH_TOKEN: ${{ secrets.HAB_AUTH_TOKEN }}
    steps:
      - name: Checkout PR code
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
          clean: true
      - name: Install Habitat CLI
        run: |
          curl https://raw.githubusercontent.com/habitat-sh/habitat/main/components/hab/install.sh | sudo bash -s -- -c stable
          sudo hab license accept
          sudo hab --version
      - name: Install Test-Kitchen-Enterprise
        run: |
          sudo -E hab pkg install --binlink --force chef/chef-test-kitchen-enterprise/2.0.3 --channel unstable
          kitchen --version
          # chef-cli is needed for some kitchen operations
          sudo -E hab pkg install --binlink --force chef/chef-cli --channel unstable
      - name: Generate fake Habitat Origin Key
        run: |
          hab origin key generate "$HAB_ORIGIN"
          # Export the keys for later use in the Docker container
          hab origin key export "$HAB_ORIGIN" > "$HAB_ORIGIN-key.tar.gz"
      - name: Build Habitat Package (for testing purpose only)
        run: |
          # Move out of default kitchen-tests dir to the repo root
          cd ..
          hab pkg build .
          source ./results/last_build.env
          cp "./results/$pkg_artifact" kitchen-tests/chef-infra-client-latest.hart
      - name: Set up HAB token file # Set up a token file to be mounted inside the dokken container
        run: |
          # Create a token file that can be mounted into containers
          echo "${{ secrets.HAB_AUTH_TOKEN }}" > hab_token
          chmod 600 hab_token
      - name: Kitchen Test
        run: |
          # Inspec integration tests are currently unstable within kitchen-test context due to hab and package changes between the projects... these will be turned back on as things finalize for a GA release
          # Use this when turning on Inspec integration tests
          # kitchen test end-to-end-${{ matrix.os }}

          # Run recipe convergence only
          kitchen converge end-to-end-${{ matrix.os }}
          kitchen destroy end-to-end-${{ matrix.os }}

#  vm_lnx_x86_64:
#    strategy:
#      fail-fast: false
#      matrix:
#        os:
#          - amazonlinux-2
#          - amazonlinux-2023
#          - almalinux-8
#          - almalinux-9
#          - almalinux-10
#          - debian-11
#          - debian-12
#          # - debian-13 # not officially supported yet, has issue with apt_repository resource due to signing changes
#          - fedora-41
#          - fedora-42
#          - opensuse-leap-15
#          - oraclelinux-8
#          - oraclelinux-9
#          - oraclelinux-10
#          - rockylinux-8
#          - rockylinux-9
#          - rockylinux-10
#          - ubuntu-2204
#          - ubuntu-2404
#    runs-on: ubuntu-22.04
#    env:
#      CHEF_LICENSE: accept-no-persist
#      KITCHEN_LOCAL_YAML: kitchen.linux.ci.yml
#    steps:
#      - name: Checkout PR code
#        uses: actions/checkout@v6
#        with:
#          ref: ${{ github.event.pull_request.head.sha }}
#          persist-credentials: false
#          clean: true
#      - name: Install Vagrant and VirtualBox
#        run: |
#          wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg
#          cat files/oracle_vbox_2016.asc | gpg --dearmor | sudo tee /usr/share/keyrings/oracle-virtualbox-2016.gpg
#          echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
#          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/oracle-virtualbox-2016.gpg] https://download.virtualbox.org/virtualbox/debian $(lsb_release -cs) contrib" | sudo tee /etc/apt/sources.list.d/virtualbox.list
#          sudo apt-get update
#          sudo apt-get install -y vagrant virtualbox-7.1
#          sudo vagrant --version
#          sudo VBoxManage --version
#      - name: Install Chef
#        uses: actionshub/chef-install@main
#      - name: Kitchen Test
#        run: |
#          # To debug, uncomment these
#          # export VAGRANT_LOG=debug
#          # export VBOX_LOG=all.e.l.f
#          # export VBOX_LOG_DEST=stdout
#          # export K_DEBUG_OPTS="--log-level=debug"
#          export LOGNAME=$USER
#          kitchen verify ${{ matrix.os }} $K_DEBUG_OPTS
