---
expeditor:
  defaults:
    buildkite:
      retry:
        automatic:
          limit: 1
      timeout_in_minutes: 30
      retry:
        automatic:
          limit: 1

steps:

#########################################################################
  # Verify - Habitat Plans
#########################################################################

- label: "Linux plan"
  commands:
    - sudo ./scripts/ci/verify-plan.sh
  expeditor:
    executor:
      linux:
        privileged: true
        single-use: true
