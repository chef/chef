---
expeditor:
  defaults:
    buildkite:
      retry:
        automatic:
          limit: 1
      timeout_in_minutes: 60

steps:
  - commands:
    - ./.buildkite/validate.release.pipeline.sh
    label: ":pipeline: Upload"
