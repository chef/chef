---
expeditor:
  defaults:
    buildkite:
      retry:
        automatic:
          limit: 1
      timeout_in_minutes: 60

steps:
  - commands:
    - .buildkite/verify.adhoc.pipeline.sh > pipeline.yaml
    - cat pipeline.yaml
    - buildkite-agent pipeline upload pipeline.yaml
    label: ":pipeline: Upload"
