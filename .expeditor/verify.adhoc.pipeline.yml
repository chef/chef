
---
expeditor:
  defaults:
    buildkite:
      retry:
        automatic:
          limit: 1
      timeout_in_minutes: 60

steps:
  - label: ":pipeline: Upload adhoc"
    commands:
      - ./.buildkite/validate.adhoc.pipeline.sh
