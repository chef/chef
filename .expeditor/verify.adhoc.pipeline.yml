---
expeditor:
  defaults:
    buildkite:
      retry:
        automatic:
          limit: 1
      timeout_in_minutes: 60

steps:
  - commands:
    - ./.buildkite/testing-ruby-installation-for-adhoc.sh
    # - /home/ec2-user/.rbenv/shims/ruby .buildkite/validate-adhoc.rb | buildkite-agent pipeline upload
    label: ":pipeline: Upload"
