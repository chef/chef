---
expeditor:
  defaults:
    buildkite:
      retry:
        automatic:
          limit: 1
      timeout_in_minutes: 60

steps:
  - commands:
    - .buildkite/validate-adhoc.rb > pipeline.validate.adhoc.yml
    - buildkite-agent pipeline upload pipeline.validate.adhoc.yml
    label: ":pipeline: Upload"
