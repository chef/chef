steps:
- task: UseRubyVersion@0
  inputs:
    versionSpec: '>= 2.5'
    addToPath: true
- script: |
    export GEM_HOME=$HOME/.gem
    gem install bundler --no-ri --no-doc
    bundle install --retry=3 --jobs=4
    sudo -E bundle exec rake spec:unit
  displayName: 'bundle exec rake'
