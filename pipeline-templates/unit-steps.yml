steps:
- task: UseRubyVersion@0
  inputs:
    versionSpec: '>= 2.5'

- script: |
    sudo gem install bundler --no-ri --no-doc
    sudo bundle install --retry=3 --jobs=4
  displayName: 'bundle install'

- script: sudo bundle exec rake spec:unit
  displayName: 'bundle exec rake'
